version: "3.8"

services:
  backend:
    build: .
    ports:
      - "1337:1337"
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - .tmp:/app/.tmp
    stdin_open: true
    tty: true
